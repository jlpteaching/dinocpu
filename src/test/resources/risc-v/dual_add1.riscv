  .text
  .align 2       # Make sure we're aligned to 4 bytes
  .globl _start
_start:
    add x1, x1, x2 # (reg[1] = reg[1] + reg[2])
    add x3, x4, x5 # (reg[3] = reg[4] + reg[5])
    nop
    nop
    nop
    nop
    nop
    nop
    nop
    nop
    nop
    nop
_last:
